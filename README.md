# Winter Tower

Ходибельный 3D-мир от первого лица: высокогорная станция на скальном гребне,
туман, снег, синий час. Цель не игра, а атмосфера - воспроизвести настроение
референсного кадра и дать по нему походить.

Играть: **https://find-the-end.fun/wintertower/**

Мир 02 витрины [find-the-end.fun](https://find-the-end.fun) - небольших
браузерных миров, в каждом из которых спрятан свой конец.

## Стек

Three.js + Vite + TypeScript. Физика - Octree из three (капсула, лестницы,
прыжок). Постпроцессинг - pmndrs/postprocessing.

Вся геометрия и поверхности собираются кодом: станция, лестницы, гребень,
интерьеры, растения и животные. Модели и карты материалов по сети не загружаются.

С 07.09.2026 на склонах появляются лиса и ворон, низкие ели, можжевельник,
рябина и сухие травы. Растения размещены пространственными инстансами,
проходы зарезервированы с учётом размеров объектов. Голоса птиц, скрип ветвей
и ледяные отзвуки синтезируются в существующем аудиоконтексте мира.
Общие генераторы и происхождение визуальных референсов — в
[world-core/ecology](https://github.com/HippocampusEvolve/world-core/tree/main/src/ecology).

## Запуск

```bash
npm install
npm run dev      # локальный сервер разработки
npm run build    # сборка в dist/
npm run preview  # посмотреть собранное
```

Вспомогательное: `npm test` проверяет свободные проходы при расстановке,
`npm run measure` проверяет компоновку и профиль лестницы, `npm run room`
проверяет раскладку помещений, `npm run octree` разбирает дерево коллизий -
во что оно обходится и то же ли получается при сборке по частям.

## Что где

| | |
|---|---|
| `src/world/` | геометрия мира: рельеф, станция, лестницы, интерьеры, материалы |
| `src/hands/` | инструменты от первого лица и их анимация |
| `src/` | игрок, взгляд, атмосфера, снег, ветер, заставка загрузки |
| `src/ecology.ts` | растения, животные и голоса природы |
| `docs/` | разбор референса, план работ, масштабы |
| `tools/` | служебные скрипты замеров |

Материалы генерируются в Worker из закреплённой версии `world-core`.

## Лицензии

Код - [MIT](LICENSE). Текстуры - CC0 с ambientCG, подробности в
[NOTICE.md](NOTICE.md).
