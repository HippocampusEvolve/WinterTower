/**
 * look.ts — взгляд уехал в ядро (`world-core/core`, `SmoothLook`): та же
 * пружина догона, те же крены в вираж и на стрейфе, тот же клевок приземления
 * и тот же `?rawlook`. Числа этого мира ставятся опциями при сборке (main.ts).
 *
 * Файл остался ради одного имени: появление мира (`awaken.ts`) поворачивает
 * голову сам и берёт отсюда тип того, кому он её поворачивает.
 */

export type { SmoothLook as Look } from 'world-core/core'
